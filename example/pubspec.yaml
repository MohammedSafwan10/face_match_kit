name: face_match_kit_example
description: Demonstrates guided enrollment and on-device verification.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  face_match_kit:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
